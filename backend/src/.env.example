# Django
DEBUG=True
DJANGO_SECRET_KEY=stub_secret_key_here
ALLOWED_HOSTS=localhost

# Toggle SQLite for local dev/testing (True), use MySQL when False
DB_USE_SQLITE=True

# Database (MySQL)
DB_NAME=playground_db
DB_USER=root
DB_PASSWORD=root
DB_HOST=localhost
DB_PORT=3306

# App-specific
ALLOWED_EMAIL_DOMAIN=@iitrpr.ac.in

# JWT lifetimes
JWT_ACCESS_LIFETIME=15
JWT_REFRESH_LIFETIME=7

# Google OAuth (stubs for now)
GOOGLE_CLIENT_ID_ANDROID=stub_android_id
GOOGLE_CLIENT_ID_WEB=stub_web_id
GOOGLE_CLIENT_SECRET_WEB=stub_web_secret
